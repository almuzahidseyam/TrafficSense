name: Build and Release

on:
  push:
    tags:
      - 'v*' # Trigger on tags matching v*, i.e. v1.0.0

permissions:
  contents: write

jobs:
  build:
    runs-on: windows-latest

    steps:
    - name: Checkout Code
      uses: actions/checkout@v4

    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '8.0.x'

    - name: Restore dependencies
      run: dotnet restore src/TrafficSense.csproj

    - name: Build and Publish
      run: dotnet publish src/TrafficSense.csproj -c Release -r win-x64 --self-contained true -p:PublishSingleFile=true -p:IncludeNativeLibrariesForSelfExtract=true -o ./publish

    - name: Zip Release
      run: Compress-Archive -Path ./publish/* -DestinationPath TrafficSense.zip

    - name: Create Release
      uses: softprops/action-gh-release@v1
      with:
        files: TrafficSense.zip
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
