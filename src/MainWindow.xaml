<ui:FluentWindow x:Class="NetMeter.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        mc:Ignorable="d"
        Title="NetMeter" Height="70" Width="180"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True" ResizeMode="NoResize"
        WindowBackdropType="Mica"
        ExtendsContentIntoTitleBar="True"
        WindowCornerPreference="Round"
        MouseLeftButtonDown="Window_MouseLeftButtonDown">
    
    <!-- Transparent Background for Dragging -->
    <Border Background="#20000000" CornerRadius="8" Margin="5" Padding="10,5">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Download -->
            <ui:SymbolIcon Symbol="ArrowDownload24" Foreground="#4CAF50" Grid.Row="0" Grid.Column="0" Margin="0,0,8,0" FontSize="16" />
            <TextBlock x:Name="TxtDownload" Text="0.00 KB/s" Grid.Row="0" Grid.Column="1" Foreground="White" FontFamily="Segoe UI, Kalpurush, Arial" FontSize="14" FontWeight="SemiBold" VerticalAlignment="Center"/>

            <!-- Upload -->
            <ui:SymbolIcon Symbol="ArrowUpload24" Foreground="#2196F3" Grid.Row="1" Grid.Column="0" Margin="0,4,8,0" FontSize="16" />
            <TextBlock x:Name="TxtUpload" Text="0.00 KB/s" Grid.Row="1" Grid.Column="1" Margin="0,4,0,0" Foreground="White" FontFamily="Segoe UI, Kalpurush, Arial" FontSize="14" FontWeight="SemiBold" VerticalAlignment="Center"/>
        </Grid>
    </Border>
</ui:FluentWindow>
